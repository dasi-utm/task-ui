services:
  task-ui:
    build:
      context: ..
      target: production
      args:
        VITE_API_URL: ${VITE_API_URL}
        VITE_SIGNALR_URL: ${VITE_SIGNALR_URL}
    ports:
      - "80:80"
    restart: unless-stopped
